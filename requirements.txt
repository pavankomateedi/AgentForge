# Clinical Co-Pilot — runtime + dev dependencies.
#
# pyproject.toml is the source of truth (hatchling build backend). This file
# mirrors it for environments that prefer `pip install -r requirements.txt`
# over the editable install (`pip install -e ".[dev]"`).
#
# Keep both files in sync when adding or upgrading a dependency.

# ---- Runtime ----
anthropic>=0.65.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
python-dotenv>=1.0.0
# Auth + sessions
bcrypt>=4.2.0
itsdangerous>=2.2.0
pyotp>=2.9.0

# ---- Dev / test ----
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
ruff>=0.7.0
